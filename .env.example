#############################
# Backend Environment Config
#############################

FLASK_SECRET_KEY=change-me
API_PORT=8000

# Redis
REDIS_URL=redis://localhost:6379/0

# AWS Base Config
AWS_REGION=us-east-1

# DynamoDB (if using local or AWS)
DYNAMO_TABLE_AUCTIONS=
DYNAMO_TABLE_USERS=

# RDS (if using SQL instead of Dynamo)
RDS_HOST=
RDS_PORT=
RDS_USER=
RDS_PASSWORD=
RDS_DB_NAME=

# Cognito (values from your user pool + app client)
# User pool ID: from User pool → "User pool ID"
COGNITO_USER_POOL_ID=us-east-1_UHhA2Am3q
# App client ID: from App clients → "Client ID"
COGNITO_CLIENT_ID=236m0jv1dnmdvddogrquhf0vc9
# Only if you created a client secret (you currently have this disabled, so leave blank)
COGNITO_CLIENT_SECRET=
# Hosted UI domain (include https, no trailing slash):
# e.g. https://<your-domain-prefix>.auth.us-east-1.amazoncognito.com
COGNITO_DOMAIN=https://us-east-1uhha2am3q.auth.us-east-1.amazoncognito.com

# IVS
AWS_IVS_CHANNEL_ARN=
AWS_IVS_INGEST_ENDPOINT=
AWS_IVS_PLAYBACK_URL=

# S3
AWS_S3_BUCKET=
AWS_S3_UPLOAD_PREFIX=uploads/

#############################
# Frontend Environment Config
#############################

# REST API base URL
VITE_API_BASE_URL=http://localhost:8000

# (Optional but recommended) WebSocket base URL
# If your backend exposes WS on the same port, you can use:
# ws://localhost:8000/ws
VITE_WS_BASE_URL=ws://localhost:8000/ws

# Cognito for frontend auth (Vite)
# Same client ID as COGNITO_CLIENT_ID above
VITE_COGNITO_CLIENT_ID=236m0jv1dnmdvddogrquhf0vc9

# Hosted UI domain (must match COGNITO_DOMAIN, include https, no trailing slash)
# e.g. https://<your-domain-prefix>.auth.us-east-1.amazoncognito.com
VITE_COGNITO_DOMAIN=https://us-east-1uhha2am3q.auth.us-east-1.amazoncognito.com

# Redirect URI configured in Cognito "Allowed callback URLs"
# Make sure this EXACT value is in the App client login-page config
VITE_COGNITO_REDIRECT_URI=http://localhost:5173/login/callback

# Where Cognito should send the user after logout
# This must be in "Allowed sign-out URLs" in your app client config
VITE_COGNITO_LOGOUT_REDIRECT_URI=http://localhost:5173/

# (Optional helpers if your TS/JS code uses them)
VITE_COGNITO_REGION=us-east-1
VITE_COGNITO_USER_POOL_ID=us-east-1_UHhA2Am3q
